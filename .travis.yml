language: generic # don't install any environment

script: bash ./deploy.sh

env:
  global:
  - ENCRYPTION_LABEL: "5c5a189d3129"
  - COMMIT_AUTHOR_EMAIL: "metodiev.martin@gmail.com"

branches:
  only:
  - prod